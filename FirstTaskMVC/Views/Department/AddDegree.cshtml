@using FirstTaskMVC.ViewModel
@model ShowViewModel
@{
    // Layout = "MultiplePages";
}
@* 
<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>AddDegree</title>
    <link href="~/lib/bootstrap/dist/css/bootstrap.css" rel=" stylesheet" />
</head>
<body> *@
    <form method="post">
        <div class="d-flex justify-content-center container-column">
            <div class=" container-column">
                @* <h1 class="p-4 bg-light border rounded shadow text-center"></h1> *@

                
                @foreach(var department in Model.Departments)
                {
                    <h3>Department Name : @department.DepartmentName</h3>
                }

@*             </div>
            <div class="d-flex justify-content-center"> *@
                
                @foreach (var course in Model.Courses)
                {
                    <h3>Course Name : @course.CourseName</h3>
                }

@*             </div>
            <div class="container-column"> *@

                
                @foreach (var studentin in Model.Departments)
                {
                    @foreach(var student in studentin.Students)
                    {
                        <h3>Student Name : @student.StudentFirstName @student.StudentLastName</h3>
                        <input type="text" name="StudentDegree.@student.StudentId" />
                    }
                }
                
           @*  </div> *@
            <input type="submit" value="add" />
        </div>
    </form>
@* </body>
</html>
 *@